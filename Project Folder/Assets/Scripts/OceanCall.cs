using UnityEngine;
using System.Collections;

public class OceanCall : MonoBehaviour {
	NetworkView nView;


	
	
	
	
	// Update is called once per frame
	void Update () {
	
	}



	void Bite()
	{
		//rb.AddForce (Vector3.forward * 3000 * Time.deltaTime);
	}

    /* backup
    [RPC]
    public void reel()
    {
        Debug.Log("Reeling!");
        rb.AddForce(rodEnd.transform.position * speed * Time.deltaTime);

    }
    */
}

